// create the maiin app
var app = angular.module("deliveryApp",[]);

/*
// config routing
app.config(function ($routeProvider, $httpProvider) {
  $routeProvider
    .when('/listdetail/:listid', {
      templateUrl: 'lists/listdetail.html',
      controller: 'ListsController'
    })
    .otherwise({
      redirectTo: "/main"
    });
});
*/
